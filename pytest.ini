[pytest]
# Add the current directory to Python path so 'coreceptus' is importable
pythonpath = .

# Directory where tests live (default is fine, but explicit here)
testpaths = tests

# Pattern to discover test files
python_files = test_*.py

# Show full tracebacks for better debugging
addopts = -ra -q

